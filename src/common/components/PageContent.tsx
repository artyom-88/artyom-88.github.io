import type { JSX } from 'react';
import { Outlet } from 'react-router-dom';

// TODO: either remove or add something custom
const PageContent = (): JSX.Element => <Outlet />;

export default PageContent;
