import Facebook from '@mui/icons-material/Facebook';
import { FC, ReactElement } from 'react';

const FacebookIcon: FC = (): ReactElement => <Facebook htmlColor='#1194F4' />;

export default FacebookIcon;
