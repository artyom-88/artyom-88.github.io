import LinkedIn from '@mui/icons-material/LinkedIn';
import { FC, ReactElement } from 'react';

const LinkedInIcon: FC = (): ReactElement => <LinkedIn htmlColor='#0A66C2' />;

export default LinkedInIcon;
