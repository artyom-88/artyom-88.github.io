import { createStyles, makeStyles } from '@material-ui/core/styles';

export default makeStyles(() =>
  createStyles({
    aboutPageBlock: {
      lineHeight: '24px',
      letterSpacing: '1px',
      wordSpacing: '1px',
    },
  })
);
