import { createStyles, makeStyles } from '@material-ui/core/styles';

export default makeStyles(() =>
  createStyles({
    pageContainer: {
      height: '100%',
      overflow: 'auto',
      width: '100%',
    },
  })
);
